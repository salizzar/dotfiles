#!/usr/bin/env bash

#
# Script loader. 
# 

mypath=${BASH_ARGV[0]%/*} 

export PATH=$PATH:$mypath

for file in `ls $mypath/bash` ; do
  source $mypath/bash/$file
done

for file in `ls $mypath/ruby` ; do
  source $mypath/ruby/$file
done

