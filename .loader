#!/usr/bin/env bash

#
# Script loader. 
# 

mypath=${BASH_ARGV[0]%/*} 

export PATH=$PATH:$mypath

if [ -s $mypath/aliases ] ; then
  source $mypath/aliases
fi

if [ -s $mypath/functions ] ; then
  source $mypath/functions
fi

if [ -s $mypath/rvm ] ; then
  source $mypath/rvm
fi

