#!/usr/bin/env bash

#
# Script loader.
#

mypath=${BASH_ARGV[0]%/*}

for file in $mypath/**/* ; do
	source $file
done

if [ -d $HOME/.config/$USER ] ; then
	for file in $HOME/.config/$USER/* ; do
		if [ -f $file ] ; then
			source $file
		fi
	done
fi

